# Press Next

About a game.

Install with command `npm run build` and develop with `npm run watch`.

## TODO

 * Adds metric:
   * Time the game
   * Count "next"
   * Number of level not found (percent)
